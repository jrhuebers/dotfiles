# Lessons

- When launching a terminal editor from Yazi into a newly selected tmux window, delaying the window selection alone does not prevent terminal input/response races. Start the editor only after the new window has been selected.
